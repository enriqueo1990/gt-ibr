# Producción — copiar como variable de entorno en Cloudflare Pages (NO commitear un .env.production real).
# Base del REST del backend WordPress: URL ABSOLUTA (en prod no hay proxy de Vite).
# Ej: https://cms.tu-dominio.org/wp-json
VITE_WP_API_URL=https://CAMBIAR-POR-BACKEND/wp-json
